function sumOfSquares(arr) {
    let sum = 0;
    for (let i = 0; i < arr.length; i++) {
        sum += arr[i] * arr[i];
    }
    return sum;
}
let numericVector = [1, 2, 3, 4];
console.log(sumOfSquares(numericVector)); // Output: 30 (1*1 + 2*2 + 3*3 + 4*4)
