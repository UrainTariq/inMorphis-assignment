function constructPattern(lines) {
    for (let i = 1; i <= lines; i++) {
        let pattern = '';
        for (let j = 1; j <= i; j++) {
            pattern += '* ';
        }
        console.log(pattern.trim());
    }
}
constructPattern(5);
