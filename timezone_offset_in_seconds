function timezone_offset_in_seconds(date) {
    return date.getTimezoneOffset() * 60;
}

// Test
let dt = new Date();
console.log(timezone_offset_in_seconds(dt)); // Example output: -19800 (for India Standard Time)
